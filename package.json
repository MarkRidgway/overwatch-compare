{
  "name": "node_scaffold",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js --use-strict",
    "watch": "DEBUG=true NODE_ENV=local nodemon --ignore 'public/' --use_strict server.js",
    "dev": "DEBUG=true NODE_ENV=local nodemon --inspect -e js,json server.js --exec 'node'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "chai": "^4.1.2",
    "chalk": "^2.1.0",
    "express": "^4.16.2",
    "express-status-monitor": "^1.0.1",
    "mocha": "^4.0.1",
    "mongoose": "^5.0.3",
    "node-fetch": "^1.7.3"
  },
  "devDependencies": {
    "morgan": "^1.9.0"
  }
}
